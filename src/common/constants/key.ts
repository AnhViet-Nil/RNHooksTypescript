import { DictionaryStringType } from './type';

const KEY: DictionaryStringType = {
  /**
   * Encrypted storage
   */
  STORAGE_TOKEN: 'STORAGE_TOKEN',
  /**
   * Events related REDUX
   */
  ACTION_STATUS_AUTHENTICATE: 'ACTION_STATUS_AUTHENTICATE',
};

export default KEY;
