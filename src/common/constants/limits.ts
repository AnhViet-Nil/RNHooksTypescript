import { DictionaryNumberType } from './type';

const LIMITS: DictionaryNumberType = {
  /**
   * Limits Phone Number
   */
  minPhoneNumberLength: 5,
  maxPhoneNumberLength: 15,
  /**
   * Limits Text
   */
  minTextLength: 30,
  maxTextLength: 30,
};

export default LIMITS;
